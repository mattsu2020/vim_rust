[package]
name = "vim_channel"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["net", "time", "macros", "io-util", "rt"] }

[lib]
name = "vim_channel"
path = "src/channel.rs"

[workspace]
members = ["rust_memfile"]
